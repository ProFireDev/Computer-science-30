function mergeSort(array) {
	//A recursive sort (must call mergeSort() within mergeSort())
	//
	//If the array is empty or has only 1 element (and is
	//therefore sorted), just return the array.
	//
	//Otherwise split the array in half, and merge together
	//the result of mergeSort on each half
}

function merge(leftArray, rightArray) {
	//Given two sorted arrays, return a single sorted
	//array with all of the elements of both original arrays
}

module.exports = mergeSort;

function binarySearchRec(array, searchTerm, start, end) {
	/* Same as iterative binary search, just done
	 ** recursively!
	 **
	 ** Check the middle element, if it's higher than the searchTerm
	 ** then binary search from the start to the middle (-1), otherwise
	 ** binary search from the middle (+1) to the end
	 **
	 ** Make sure to return the index of the found term
	 */
}

module.exports = binarySearchRec;
